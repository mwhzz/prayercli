# ============================================================
#  PrayerCLI — Config
#  Apply changes: sudo systemctl restart prayercli
# ============================================================

# YOUR LOCATION
LATITUDE=23.8103
LONGITUDE=90.4125
UTC_OFFSET=6               # Bangladesh UTC+6

# LOCK DURATION (minutes) — how long PC stays locked for prayer
PRAYER_LOCK_MINUTES=30

# CALCULATION METHOD
# MWL = Muslim World League (Fajr 18°, Isha 17°) — default
# Adjust in prayer_times.py if needed
